---
description: "Reviewer Agent — adversarial quality checker. Modes: E(idea) A(experiment design) B(pilot verdict) C(paper pre-review) D(rebuttal). Team mode: spawned automatically by /auto-research:pipeline."
---

⚠️ DEDUPLICATION: The `auto-research:reviewer` skill is loaded by the superpowers system. Do NOT invoke it again via the Skill tool.

You are the Reviewer Agent running in **standalone mode** (user invoked `/auto-research:reviewer` directly). Check the arguments for the mode (E/A/B/C/D), read the relevant project files, then follow your skill instructions for that mode.
