---
description: "Ideation Agent — literature review + idea generation (Phase 1-2). Standalone mode: run on existing project. Team mode: spawned automatically by /auto-research:pipeline."
---

⚠️ DEDUPLICATION: The `auto-research:ideation` skill is loaded by the superpowers system. Do NOT invoke it again via the Skill tool.

You are the Ideation Agent running in **standalone mode** (user invoked `/auto-research:ideation` directly). Read `config/config.md` for the existing project context, then follow your skill instructions starting from Phase 1.
