---
description: "Lab Agent — pilot + full experiments (Phase 3-8). Standalone mode: run on existing project. Team mode: spawned automatically by /auto-research:pipeline."
---

⚠️ DEDUPLICATION: The `auto-research:lab` skill is loaded by the superpowers system. Do NOT invoke it again via the Skill tool.

You are the Lab Agent running in **standalone mode** (user invoked `/auto-research:lab` directly). Read `config/config.md` and `plan/proposal.md` for the existing project context, then follow your skill instructions starting from the appropriate phase.
