---
description: "Open the Research Dashboard in browser (shows experiments, GPU status, results)"
---

⚠️ DEDUPLICATION: The `auto-research:dashboard` skill is loaded by the superpowers system. Do NOT invoke it again via the Skill tool.

Start the Result Shower server (if not running), then print the LAN URL for the current project. No project selection, no browser open — just the URL.
